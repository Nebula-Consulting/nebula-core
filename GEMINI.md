# Gemini Rules

- **Apex Class Creation**: Always create a matching `-meta.xml` file when creating a new Apex class (`.cls`). Use the `sourceApiVersion` from `sfdx-project.json` (currently 59.0) or match existing files if different.
